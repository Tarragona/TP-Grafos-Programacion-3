package grafo;

public class Vertice {
	
	//String nombre;
	int nombre;
	int numVertice;
	
	/*public Vertice (String Nombre, int num) {
		
		nombre = Nombre;
		numVertice = num;
	}*/

	public Vertice (int Nombre, int num) {
		
		nombre = Nombre;
		numVertice = num;
	}
	
	/*public String nomVertice() {
		
		return nombre;
	}
	
	public boolean esIgual(Vertice n) {
		
		return nombre.equals(n.nombre);
	}*/
	
	public int nomVertice() {
		
		return nombre;
	}
	
	public boolean esIgual(Vertice n) {
		
		return nombre == n.nombre;
	}
}
